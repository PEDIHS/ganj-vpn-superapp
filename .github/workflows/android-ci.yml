name: Android CI

on:
  pull_request:
    paths:
      - "apps/android/**"
      - ".github/workflows/android-ci.yml"
      - ".github/scripts/android-product-guard.sh"
  push:
    branches: [main]
    paths:
      - "apps/android/**"
      - ".github/workflows/android-ci.yml"
      - ".github/scripts/android-product-guard.sh"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Test, lint and build
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      GRADLE_OPTS: >-
        -Dorg.gradle.daemon=false
        -Dorg.gradle.logging.stacktrace=all
        -Dkotlin.incremental=false
    steps:
      - name: Check out source
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          cmdline-tools-version: "14742923"

      - name: Install required Android SDK packages
        shell: bash
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6.3.0
        with:
          gradle-version: "9.4.1"
          cache-provider: basic
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Enforce subscription-only production surface
        working-directory: .
        shell: bash
        run: bash .github/scripts/android-product-guard.sh

      - name: Run unit tests, Android lint and debug build
        working-directory: apps/android
        shell: bash
        run: >-
          gradle --no-daemon --stacktrace --warning-mode all
          testDebugUnitTest
          lintDebug
          :app:assembleDebug

      - name: Verify debug APK and generate checksum
        shell: bash
        run: |
          set -Eeuo pipefail
          apk="apps/android/app/build/outputs/apk/debug/app-debug.apk"
          test -s "$apk"
          unzip -tqq "$apk"
          sha256sum "$apk" > "$apk.sha256"
          test "$(stat -c '%s' "$apk")" -lt 209715200

      - name: Generate source and dependency SBOM
        uses: anchore/sbom-action@v0.24.0
        with:
          path: apps/android
          format: spdx-json
          output-file: apps/android/app/build/reports/ganj-vpn-android.spdx.json
          upload-artifact: false
          upload-release-assets: false

      - name: Validate SBOM
        shell: bash
        run: |
          set -Eeuo pipefail
          sbom="apps/android/app/build/reports/ganj-vpn-android.spdx.json"
          test -s "$sbom"
          jq -e '.spdxVersion and (.packages | length > 0)' "$sbom" >/dev/null

      - name: Upload debug APK
        uses: actions/upload-artifact@v7
        with:
          name: ganj-vpn-debug-${{ github.sha }}
          path: |
            apps/android/app/build/outputs/apk/debug/app-debug.apk
            apps/android/app/build/outputs/apk/debug/app-debug.apk.sha256
          if-no-files-found: error
          retention-days: 7

      - name: Upload test, lint and SBOM evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ganj-vpn-quality-${{ github.sha }}
          path: |
            apps/android/**/build/reports/**
            apps/android/**/build/test-results/**
          if-no-files-found: warn
          retention-days: 14
